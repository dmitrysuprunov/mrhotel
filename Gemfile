source 'https://rubygems.org'

#Rails Stack
gem 'rails', '4.1.6'                              # Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'pg'                                          # Use postqre sql
gem 'sass-rails', '~> 4.0.3'                      # Use SCSS for stylesheets
gem 'uglifier', '>= 1.3.0'                        # Use Uglifier as compressor for JavaScript assets
gem 'coffee-rails', '~> 4.0.0'                    # Use CoffeeScript for .js.coffee assets and views
gem 'jquery-rails'                                # Use jquery as the JavaScript library
gem 'turbolinks'                                  # Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'jbuilder', '~> 2.0'                          # Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder

#Customs
gem 'puma'                                        # Puma is a simple, fast, threaded, and highly concurrent HTTP 1.1 server for Ruby/Rack applications.
gem 'jquery-ui-rails'                             # This gem packages the jQuery UI assets (JavaScripts, stylesheets, and images) for the Rails asset pipeline, so you never have to download a custom package through the web interface again.
gem 'devise'                                      # Flexible authentication solution for Rails with Warden
gem 'bootstrap-sass'                              # Twitter's Bootstrap, converted to Sass and ready to drop into Rails or Compass
gem 'bootstrap-glyphicons'                        # Showing bootstrap glyph icon for view
gem 'autoprefixer-rails'                          # Autoprefixer is a tool to parse CSS and add vendor prefixes to CSS rules using values from the Can I Use. This gem provides Ruby and Ruby on Rails integration with this JavaScript tool.
gem 'andand'                                      # Object#andand’s functionality is also available as part of the RewriteRails plug-in. If you are using Ruby on Rails, please consider using RewriteRails instead of the andand gem.
gem 'haml'                                        # Haml is a templating engine for HTML. It’s designed to make it both easier and more pleasant to write HTML documents, by eliminating redundancy, reflecting the underlying structure that the document represents, and providing an elegant syntax that’s both powerful and easy to understand.
gem 'rails_admin'                                 # RailsAdmin is a Rails engine that provides an easy-to-use interface for managing your data.
gem 'rails_admin_globalize'                       # Rails Admin plugin to manage Globalize translations
gem 'paper_trail'                                 # Track changes to your models' data. Good for auditing or versioning.
gem 'carrierwave'                                 # Upload files in your Ruby applications, map them to a range of ORMs, store them on different backends.
gem 'rmagick', :require => false                  # RMagick is an interface between Ruby and ImageMagick.
gem 'faker'                                       # Faker, a port of Data::Faker from Perl, is used to easily generate fake data: names, addresses, phone numbers, etc.
gem 'factory_girl_rails'                          # Factory_girl_rails provides integration between factory_girl and rails 3 (currently just automatic factory definition loading)
gem 'simple_form'                                 # Forms made easy!
gem 'inherited_resources'                         # Inherited Resources speeds up development by making your controllers inherit all restful actions so you just have to focus on what is important.
gem 'kaminari'                                    # Kaminari is a Scope & Engine based, clean, powerful, agnostic, customizable and sophisticated paginator for Rails 3+
gem 'kaminari-bootstrap'                          # Kaminari visual bootstrapping
gem 'cancancan'                                   # Continuation of the simple authorization solution for Rails which is decoupled from user roles. All permissions are stored in a single location.
gem 'foreigner'                                   # Adds helpers to migrations and dumps foreign keys to schema.rb
gem 'redis-rails'                                 # Redis for Ruby on Rails
gem 'russian'                                     # Russian translate
gem 'email_validator'                             # An email validator for Rails 3+. See homepage for details: http://github.com/balexand/email_validator
gem 'breadcrumbs_on_rails'                        # BreadcrumbsOnRails is a simple Ruby on Rails plugin for creating and managing a breadcrumb navigation for a Rails project. It provides helpers for creating navigation elements with a flexible interface.
gem 'chosen-rails', '1.1.0'                       # Chosen is a library for making long, unwieldy select boxes more user friendly.
gem 'bootstrap-datepicker-rails'

group :development do
  gem 'capistrano'                                # Capistrano is a utility and framework for executing commands in parallel on multiple remote machines, via SSH.
  gem 'capistrano-rvm'                            # RVM integration for Capistrano
  gem 'capistrano-rails'                          # Rails specific Capistrano tasks
  gem 'capistrano-rails-console'                  # Rails specific Capistrano tasks of console
  gem 'capistrano3-puma'                          # Puma integration for Capistrano 3
  gem 'capistrano-sidekiq'                        # Sidekiq integration for Capistrano
  gem 'better_errors'                             # More Flexible Errors Pages
  gem 'quiet_assets'                              # Disable Assets Logger
  gem 'rails_best_practices'                      # Is a code metric tool to check the quality of rails codes.
  gem 'jazz_hands',
      :github => 'nixme/jazz_hands',
      :branch => 'bring-your-own-debugger'        # Spending hours in the rails console? Spruce it up and show off those hard-working hands! jazz_hands replaces IRB with Pry, improves output through awesome_print, and has some other goodies up its sleeves.
end